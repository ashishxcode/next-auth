'use client';
import React from 'react';
import { UserDataProps } from '@/types/users';

export const SettingForm = ({ userData }: { userData: UserDataProps }) => {
	return <div>Setting form</div>;
};
